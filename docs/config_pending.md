# Незаполненные значения config.yaml

Сгенерировано `tools/validate_config.py --checklist`; не редактировать руками — файл перезаписывается.

Заполнять значениями, которые реально видны на странице. К каждому — откуда взято и когда. Если значение относится к конкретному аккаунту, а не к базовому тарифу, это надо написать.

## Комиссии тейкера

_Тарифная страница биржи, раздел спота, уровень без VIP и без скидки за удержание токена биржи. Записать ставку тейкера как долю (0,1 % -> 0.001). Осторожно: у Kraken две схемы, нужна Kraken Pro по стакану, а не мгновенная покупка в приложении._


## Сети вывода

### BTC — Bitcoin (`BTC`)

- [ ] `confirmations` — Там же или в справке биржи: сколько подтверждений сети требуется для зачисления. Это политика биржи, у каждой своя.

      - okx: ______

### ETH — Ethereum (`ERC20`)

- [ ] `confirmations` — Там же или в справке биржи: сколько подтверждений сети требуется для зачисления. Это политика биржи, у каждой своя.

      - okx: ______

### ETH — Arbitrum One (`ARBITRUM`)

- [ ] `confirmations` — Там же или в справке биржи: сколько подтверждений сети требуется для зачисления. Это политика биржи, у каждой своя.

      - kraken: ______
      - okx: ______

### SOL — Solana (`SOL`)

- [ ] `confirmations` — Там же или в справке биржи: сколько подтверждений сети требуется для зачисления. Это политика биржи, у каждой своя.

      - kraken: ______
      - okx: ______

---

Всего значений: 6. Строка вида `withdrawal_fee -> binance, bybit, kraken, okx` - это четыре числа, а не одно.
